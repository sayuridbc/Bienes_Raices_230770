extends ../layout/index

block contenido
    div.py-2
    h1.text-4xl.my-10.font-extrabold.text-center.text-azulMunsell Bienes
        span.front-normal.text-azulClaro Raices
    h2.text-center.text-2xl.font-extrabold.text-azulPrusia= pagina

    div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto") 
        p(class=error ? 'bg-red-500 py-2 px-5 rounded-lg w-full font-bold text-center' : 'bg-green-500 py-2 px-5 rounded-lg w-full font-bold text-center')= mensaje

        if !error
            a(href="/auth/login" class="text-center font-bold text-sm text-slate-600 uppercase mt-10 block") Ya puedes Iniciar Sesión